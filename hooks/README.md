# Hooks

Claude Code hooks are not a native Codex surface. This package therefore does not install the original Claude hook workflow.

For local repository enforcement, prefer normal Git hooks, CI checks, or Codex prompt/skill workflows. Optional sample git hooks are in `scripts/codex-git-hooks/` when present.
