interface:
  display_name: "Continuous Agent Loop"
  short_description: "Patterns for continuous autonomous agent loops with quality gates, evals, and recovery controls. Use when Codex needs support for: Patterns for..."
  default_prompt: "Use the continuous-agent-loop skill to handle this task. Inspect relevant local files first, use mock or dry-run mode for external integrations, and report missing dependencies explicitly."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    []
