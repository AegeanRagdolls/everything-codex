interface:
  display_name: "Continuous Learning"
  short_description: "Automatically extract reusable patterns from Claude Code sessions and save them as learned skills for future use. Use when Codex needs support..."
  default_prompt: "Use the continuous-learning skill to handle this task. Inspect relevant local files first, use mock or dry-run mode for external integrations, and report missing dependencies explicitly."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    []
